import 'board_size.dart';
import 'difficulty.dart';

class NewGameSelection {
  final Difficulty difficulty;
  final BoardSize boardSize;

  const NewGameSelection(this.difficulty, this.boardSize);
}
